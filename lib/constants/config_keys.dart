/// Constants for config keys returned by the API
class ConfigKeys {
  // Private constructor to prevent instantiation
  ConfigKeys._();

  /// URL for Discord community server
  static const String discordUrl = 'DISCORD_URL';

  /// Add other config keys here as needed
}
